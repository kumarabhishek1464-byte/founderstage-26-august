/**
 * Encoding primitives that are not specific to any one caller.
 *
 * ```ts
 * import { decodeBase64Url, encodeBase64Url } from '@/core/encoding';
 * ```
 */
export { decodeBase64Url, encodeBase64Url } from './base64url';
