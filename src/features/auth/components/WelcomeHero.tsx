/**
 * `WelcomeHero` — floating member portrait collage matching Screen 1.
 */
import { HeroPersonaCollage } from '@/core/design-system';

export function WelcomeHero() {
  return <HeroPersonaCollage />;
}
